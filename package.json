{
  "name": "voca-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "build": "node build.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check build.mjs",
    "test": "node --test scripts/release-config.test.mjs"
  }
}
